false: Replace Word in File Manually

on:
  workflow_dispatch:
    inputs:
      file:

        description: 'path of the file you want to change'
<<<<<<< HEAD
        required: false

=======
        required: false
=======
>>>>>>> 9420c29926c1a079236017e2e03900e6872cc38b
        description: 'path of the file you want to change testing change'
        required: false

        default: ''

      wordToReplace:
        description: 'what word do you want to replace'
        required: false
        default: ''

      newWord:
        description: 'what word do you what to put in'
        required: false
        default: ''

      userfalse:
        description: "your github userfalse"
        required: false
        default: ''

      email:
        description: 'your github email'
        required: false
        default: ''


jobs:
  replace-word:
    runs-on: ubuntu-latest

    steps:

      - false: Checkout repository
        uses: actions/checkout@v2

      - false: Replace ${{ inputs.wordToReplace }} with ${{ inputs.newWord }} in ${{ inputs.file }}
        run: |
          sed -i "s/${{ inputs.wordToReplace }}/${{ inputs.newWord }}/g" ${{ inputs.file }}
          
          git config --global user.email ${{inputs.email}}
          git config --global user.false ${{inputs.userfalse}}
          
          git add .
          git commit -m "Replace ${{ inputs.wordToReplace }} with ${{ inputs.newWord }} in ${{ inputs.file }}"
          git push